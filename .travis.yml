language: python
python:
    - 2.7
before_install:
  # Start x session for splinter. Ref http://about.travis-ci.org/docs/user/gui-and-headless-browsers/
    - "export DISPLAY=:99.0"
    - "sh -e /etc/init.d/xvfb start"
install:
  # Dependencies
    - test -d pip.cache || git clone git://github.com/qdamian/depict.pip.cache pip.cache
    - pip install --no-index --find-links=pip.cache -r requirements.txt
    - pip install -r requirements.txt --use-mirrors
    - pip install -r dissect-requirement.txt --use-mirrors
                                             --index-url=https://testpypi.python.org/pypi
                                             --extra-index-url=https://pypi.python.org/pypi
    - npm install
  # Workaround https://github.com/travis-ci/travis-cookbooks/issues/155
    - sudo rm -rf /dev/shm && sudo ln -s /run/shm /dev/shm
script:
  # Lint
    - pylint --errors-only --rcfile=.pylintrc python/depict/
    - npm run-script lint
  # Unit test
    - nosetests --config .noserc python/depict
    - npm test
  # Build
    - rm -rf build
    - python python/setup.py sdist
  # Install
    - python python/setup.py install
  # Functional test
    - behave test/integration/features --tags=~extended
    - behave test/system/features --tags=~extended
after_success:
  # Report
    - coveralls
  # Update gh-pages
    - bash update-gh-pages.sh
